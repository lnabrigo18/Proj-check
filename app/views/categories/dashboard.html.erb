<table>
<% @tasks.each do |task| %>
    <% if task.due_date == Date.today %>
    <tr>
    <td><%=task.name%></td>
    <td><%= task.due_date.strftime("%m/%d") %></td>
    <td><%= link_to "Go back", category_path(task.category_id) %></td>
    </tr>
  <% end %>
  <% end %>
</table>